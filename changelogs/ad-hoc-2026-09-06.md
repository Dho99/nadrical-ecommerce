# Changelog — Ad-hoc (sesi lanjutan)

**Tanggal:** 2026-09-06
**Status:** Implementasi

## 1. Bersih-bersih area back-office duplikat
- Rename seluruh prefiks `Admin*` → `Customer*` (folder `modules/customer`, layout/routes), path `/admin` → `/customer`.
- Setelah itu area tsb **dihapus total** karena back-office dibuat di web terpisah:
  - Hapus `CustomerLayout/Sidebar/Topbar`, `CustomerGuard`, 8 `Customer*Page`, `modules/customer/`.
  - Hapus blok route `/customer` di `src/app/routes/index.tsx`.
- Tanpa referensi tersisa (grep 0 match).

## 2. Wishlist
- Toast "Saved to wishlist" aksi **Lihat** → redirect `/profile/wishlist` (bukan buka dialog).
- `WishlistDialog.tsx` + `utils/openWishlist.ts` dihapus (sudah tidak terpasang).

## 3. Order history mock-only (`/profile/orders`)
- `profileService.getOrderHistory` tidak lagi `api.get('/ecommerce/orders')`.
- Sumber: seed `generateUserOrders(email)` + order lokal sesi (best-effort `orderRepository.list()` dibungkus try/catch) → selalu tampil saat backend off.

## 4. Invoice PDF v2 (Blue print)
- Dep `jspdf` ditambahkan.
- `modules/orders/services/invoice.service.ts`: `InvoiceData` snapshot (currency aktif saat download), logo `/public/logo.svg` (canvas rasterize + cache).
- Layout blueprint: header brand+tagline, metadata Invoice No/Issued, kolom CUSTOMER↔ORDER, ITEM SUMMARY tabel (Product/Qty/Unit Price/Total + subline Variant), SUMMARY, PAYMENT Information (badge warna status), SHIPPING Information (kurir/resi/alamat), footer.
- `InvoiceButton` thin wrapper + prop `email`; `OrderDetailPage` pass `user.email`.

## 5. Refund jadi halaman sendiri (tanpa localStorage)
- Route `/profile/orders/:id/refund` → `RefundPage`.
- `RefundRequestForm` (halaman penuh): order info, reason (highlight), note, **upload foto & video** (multi, preview, remove).
- Status refund readonly bila sudah ada (`StatusView`).
- `refund.service.ts` diubah **in-memory** (bukan localStorage) — siap di-wire ke API berikutnya.
- `RefundDialog` dihapus; `OrderDetailPage` pakai Link ke halaman refund.

## 6. Order list UI
- `/profile/orders` tab status: segmented pill custom, aktif `bg-[#f34e7b] text-white`, dot warna per status, count chip; scroll mobile.
- Tombol **View details** solid primary (`bg-[#f34e7b]`) + `ArrowRight`.

## 7. Cancel order tanpa browser API
- `OrderDetailPage`: `window.confirm` diganti `AlertDialog` (ui/alert-dialog), state `cancelling` disable aksi.

## 8. Nav warna logo
- `SiteHeader`: item regular light `#39258d` (samakan wordmark logo), dark putih; aktif **pink `#f34e7b`** bold (dua tema).

## Keputusan
- PDF immutable per download (snapshot currency saat generate).
- Tidak localStorage utk refund karena akan disambungkan ke API.

## Catatan / error pre-existing
- Error TS di luar scope masih ada:
  - `src/app/routes/ProductRatingsPage.tsx` (EmptyState butuh `description`).
  - `src/modules/products/services/review.service.ts` (property `items` pada union response).
  - (resolver RHF ProductForm di `modules/customer` sudah hilang ikut modul dihapus).
- Belum diimplementasi: Apple OAuth real, map tracking, live courier, wire refund/invoice ke API.
